// function suma (x, y) {
//     return x + y;
// };

const suma = function (x, y) {
    return x + y;
};

const resta = (x, y) => x - y;


// Hacer una función que salude al usuario con su nombre
function saludarNombre(nombre){
    return "Hola " + nombre;
};





module.exports = {
    suma,
    resta,
    saludarNombre
}

// module.exports = {
//     suma
// }
